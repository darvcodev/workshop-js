const activo = true;
let mensaje = ""

if (activo === true) {
  mensaje = "Activo";
} else {
  mensaje = "Inactivo";
}

// Operador Ternario
const mensaje = activo ? "Activo" : "Inactivo";

const mensaje = data.lenght < 0 ? <div>Código</div> : <div>Cargando..</div>;

const mensaje = isLogin && 'Si esta login'

